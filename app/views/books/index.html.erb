<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th class="col-id"><%= link_to "ID", :sort => "id" %></th>
      <th class="col-title"><%= link_to "Title", :sort => "title" %></th>
      <th class="col-author"><%= link_to "Author", :sort => "author" %></th>
      <th class="col-cuisine"><%= link_to "Cuisine", :sort => "cuisine" %></th>
    </tr>
  </thead>
  
  <% @books.each do |book| %>
    <tr>
      <td><%= book.id %></td>
      <td><%= book.title %></td>
      <td><%= book.author %></td>
      <td><%= book.cuisine %></td>
    </tr>
    <% end %>
  </table>